fn main(): int {
    let x: int = 10;

    while (x == 10) {
        if (x == 10) {
            break;
        } else {
            continue;
        }
    }

    while (x == 10) {
        x = 20;
    }

    x = 20;
    return x;
}